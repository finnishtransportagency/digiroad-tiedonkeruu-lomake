{
  "name": "dr-tiedonkeruu",
  "description": "Digiroad Tiedonkeruu-lomake",
  "version": "1.0.0",
  "scripts": {
    "build:ui": "cd frontend && npm ci && npm run build && cd ..",
    "cp:ui": "cp -r frontend/build serverless/ui",
    "deploy:serverless": "cd serverless && npm i && serverless deploy --config serverless-waf.yml && serverless deploy --verbose && cd .."
  },
  "keywords": [
    "aws",
    "serverless",
    "Digiroad",
    "DRTiedonkeruu",
    "Tiedonkeruu-lomake"
  ]
}
