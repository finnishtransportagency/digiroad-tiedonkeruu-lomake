{
  "name": "dr-tiedonkeruu",
  "description": "Digiroad Tiedonkeruu-lomake",
  "version": "1.0.0",
  "scripts": {
    "build:ui": "cd frontend && npm ci && npm run build && cd ..",
    "cp:ui": "cp -r frontend/build serverless/ui",
    "deploy:serverless": "cd serverless && npm i && sls deploy --config serverless-waf.yml && npm run set:wafid && echo WEB_ACL_ID && sls deploy --verbose && cd .."
  },
  "keywords": [
    "aws",
    "serverless",
    "Digiroad",
    "DRTiedonkeruu",
    "Tiedonkeruu-lomake"
  ]
}
