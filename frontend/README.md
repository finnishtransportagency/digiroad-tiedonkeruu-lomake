# Frontend: web client

## Running locally

1. Install dependecies by running
   `npm install`
2. Provide API URL in `.env`-file
   `REACT_APP_API_ENDPOINT=<http://API_URL:PORT>`
3. Start web client in development mode by running
   `npm start`