{
  "name": "dr-tiedonkeruu-email-notification-service",
  "description": "API for DRTiedonkeruu. Handles form data from DRTiedonkeruu web-client and sends verification email to the user and DR-operator.",
  "version": "1.0.0",
  "dependencies": {
    "aws-sdk": "^2.1437.0",
    "zod": "^3.22.2"
  },
  "scripts": {
    "ping": "serverless invoke local --function ping",
    "offline": "serverless offline --httpPort 3003",
    "cp:ui": "cp -r ../frontend/build ./build",
    "package": "npm run cp:ui && serverless package && rm -r ./build"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.101",
    "serverless": "^3.21.0",
    "serverless-offline": "^9.1.6",
    "serverless-plugin-typescript": "^2.1.2",
    "serverless-s3-sync": "^3.1.0",
    "typescript": "^4.7.4"
  }
}
